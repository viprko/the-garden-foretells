spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: file:///D:/projects/1.config-servers/the-garden-foretells-config
          default-label: main
          skipSslValidation: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka

server:
  port: 8888
