spring:
  cloud:
    config:
      uri: http://localhost:8888
      name: auth-service
      profile: default
      label: main
  application:
    name: auth-service
logging:
  level:
    org.springframework.cloud.config: DEBUG
